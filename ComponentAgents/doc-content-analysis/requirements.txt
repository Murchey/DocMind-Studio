python-docx>=0.8.11
pdf2docx>=0.5.6
PyMuPDF>=1.23.0
Pillow>=9.0
pywin32>=306; sys_platform == "win32"

# OCR 依赖（处理扫描型/图片型知网 PDF 必须）
# 方案1: Tesseract（推荐，需系统安装 Tesseract + 中文语言包）
pytesseract>=0.3.10

# 方案2: PaddleOCR（中文识别精度更高，但体积较大）
# paddleocr>=2.6
# paddlepaddle>=2.4

# 方案3: EasyOCR（多语言，无需系统依赖）
# easyocr>=1.6
